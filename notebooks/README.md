# Notebooks (`notebooks/`)

Exploratory **Jupyter** work: ad-hoc analysis, plotting, or dataset inspection. Nothing here is required for the CLI pipelines in `scripts/`.

Add a short notebook-level comment in the first cell describing data sources (`eval_set/...` → `DATA/eval_set/...`, or `results/from_*` snapshots) and whether outputs are committed elsewhere.
